import common from './common'
import { register } from './register'
import { shoppingLists } from '@/i18n/en/shoppingLists'

const translations = {
  common,
  register,
  shoppingLists,
}

export default translations
